import {general} from '~/selectors/general/general';
import {films} from '~/selectors/films/films';
import {user} from '~/selectors/user/user';
import {comments} from '~/selectors/comments/comments';

export const selectors = {
  general,
  films,
  user,
  comments,
};
